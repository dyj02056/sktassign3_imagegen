# submission-note.md — 짤·카드 스튜디오 수정 기록

## 개요
- 원본: pass_condition.md (T03-C01 ~ T03-C32)
- 목표: 모든 통과 기준 충족
- 진행: (A) 코드 결함 수정 → (B) 제출문 초안 → (C) 제출 패키지 구성
- 결과물: https://sktassign3-imagegen.vercel.app/

---

## (A) 코드 결함 수정

### A-1. 검토에서 확인된 코드 결함 목록
1. **T03-C11~C13, C22 (미리보기 = 저장 파일 불일치 위험)**
   - 원인: `downloadImage()` 내부에서 `wrapText()`가 전역 `ctx`로 폰트 측정.
   - 조치: `wrapTextWith(context, text, maxWidth)` 로 리팩터링하여
     저장 시에는 반드시 저장용 컨텍스트로 측정.

2. **T03-C19 (카드4) 템플릿 "수정" UI 부재**
   - 조치: 각 템플릿 항목에 **이름 더블클릭 인라인 편집** +
     **"덮어쓰기"(현재 상태로 갱신)** 두 경로 제공.

3. **T03-C21 (20MB 이상 이미지) 성능 위험**
   - 조치: 로드 직후 최대 변 **2400px 로 다운스케일**한 별도 canvas 재렌더.
     원본은 보관하지 않음. 메타데이터도 이 과정에서 자연 제거.

4. **T03-C25 (연속 다운로드 파일명 충돌)**
   - 조치: `downloadSeq` + `Date.now()` + 4자리 랜덤 결합.

### A-2. 추가 보강
5. **T03-C29 (개인정보 0건)** — 파일명을 `imgInfo` 텍스트가 아닌
   **title 속성으로만** 노출. 표시는 "✅ 이미지 로드됨 (W×H)".
6. **T03-C16 (이모지 컬러 렌더)** — 폰트 스택에
   `"Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji"` 추가.
7. **T03-C23 (창 크기 변경 시 비율 유지)** — `canvas-wrap` 에
   `max-height:70vh` + `object-fit:contain` 추가, 9:16 세로 화면 대응.

### A-3. 수정 후 판정 변화
| 기준 | 이전 | 이후 |
|---|---|---|
| T03-C11~13 | ⚠️ | ✅ |
| T03-C16 | ⚠️ | ✅(크롬 기준) |
| T03-C19(카드4) | ⚠️ | ✅ |
| T03-C21 | ⚠️ | ✅ |
| T03-C22 | ⚠️ | ✅ |
| T03-C23 | ✅ | ✅(강화) |
| T03-C25 | ⚠️ | ✅ |
| T03-C29 | ⚠️ | ✅ |

### A-4. A 단계 완료 체크
- [x] wrapText 컨텍스트 인자화
- [x] 이모지 폰트 폴백
- [x] 템플릿 수정(이름 + 덮어쓰기)
- [x] 이미지 다운스케일 2400px
- [x] 다운로드 파일명 충돌 방지
- [x] 파일명 마스킹
- [x] 캔버스 표시 영역 제약

---

## (B) 제출문 초안

### B-1. 짧은 확인 방법 4줄 (T03-C31)

**① 어디로 가나요**
- 결과물: https://sktassign3-imagegen.vercel.app/
- 소스:   https://github.com/<본인계정>/<저장소명>
- 두 주소 모두 계정·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이
  시크릿 창에서 열린다.

**② 3단계 이내 무엇을 하나요**
1. 좌측 상단 **"PNG · JPEG 불러오기"** 를 눌러 PNG 또는 JPEG 이미지를 고른다.
2. 가운데 문구 상자에 **"안녕 😀"** 를 입력하고 크기 슬라이더를 움직인다.
3. 우측 상단 **"이미지 다운로드"** 를 누른다.

**③ 무엇이 보이면 통과인가요**
- 1단계 후: 좌측 하단 라벨이 **"✅ 이미지 로드됨 (W×H)"** 로 바뀌고,
  가운데 캔버스에 이미지가 즉시 나타난다.
- 2단계 중: 슬라이더를 움직이는 **즉시** 캔버스 위 문구의 크기·위치가 따라 움직인다.
- 3단계 후: 브라우저 다운로드 폴더에
  `jjalm-card_1x1_<타임스탬프>_<seq>_<rnd>.png` 파일이 저장되고,
  그 파일을 열면 **화면 미리보기와 문구 위치·줄바꿈·잘림이 동일**하다.

**④ 안 될 때 무엇이 보이나요**
- 지원하지 않는 파일(.txt, .gif 등)을 넣으면 화면 하단에 빨간 토스트로
  **"지원하지 않는 형식: <MIME> (PNG·JPEG·WebP만)"** 이 뜨고,
  좌측 라벨은 **"❌ 지원하지 않는 파일 (기존 작업 유지)"** 로 바뀐다.
  → 캔버스의 기존 작업은 그대로 남아 있어야 한다.
- 손상된 JSON 을 가져오면 **"JSON 문법 오류 — 기존 템플릿 유지"** 토스트가 뜨고,
  템플릿 개수 라벨(`N개 저장됨`)의 숫자가 **변하지 않는다.**
- 필수 항목(templates)이 빠진 JSON 은
  **"필수 항목(templates) 누락 — 기존 템플릿 유지"** 토스트가 뜨고,
  마찬가지로 개수가 유지된다.

---

### B-2. AI와 나의 판단 3줄 (T03-C32)

**① AI에게 맡긴 일**
- HTML/CSS/JS 단일 파일 구조 설계와 초기 스캐폴딩
  (좌측 편집 · 가운데 캔버스 · 우측 비율/템플릿 3열 레이아웃).
- `wrapTextWith(context, text, maxWidth)` 줄바꿈 알고리즘 초안.
- `computeImageRect` 의 cover-fit 계산식.
- localStorage 템플릿 CRUD, JSON 내보내기/가져오기, 다운로드 파이프라인 뼈대.
- pass_condition.md 의 T03-C01~C32 각 항목과 코드 라인의 1차 대조표 작성.

**② 학생이 직접 판단한 일**
- **미리보기와 저장 파일의 폰트 측정이 반드시 같은 컨텍스트에서
  이루어져야 한다**는 결정. (원 AI 초안은 `wrapText` 가 전역 `ctx` 를 쓰고
  저장 시에도 그 값을 재사용 → 캔버스 크기가 다르면 폰트 힌팅이 어긋날 수
  있다는 근거로 `wrapTextWith(octx, ...)` 로 강제 분리.)
- **20MB 이상 이미지를 원본 그대로 보관하지 않고 최대 변 2400px 로
  다운스케일**한 뒤 state 에 보관하도록 결정. (출력 최대치가 1920px 이므로
  2400px 이면 충분하고, 이 과정에서 EXIF/위치정보가 자연히 제거되어
  T03-C28 도 함께 충족된다고 판단.)
- **템플릿 "수정" 을 두 경로로 제공**: 이름 더블클릭 인라인 편집 +
  현재 상태로 덮어쓰기. (T03-C19 카드4 의 "수정" 을 단일 버튼으로만
  해석하면 실사용에서 "무엇을 수정하는가" 가 모호해진다고 판단.)
- **다운로드 파일명에 `downloadSeq` + 4자리 랜덤 결합**.
  (`Date.now()` 만으로는 같은 ms 내 연속 다운로드에서 충돌 가능 — T03-C25.)
- **파일명을 `imgInfo` 에 그대로 노출하지 않고 title 속성으로만** 두기로 결정.
  (스크린샷 제출 시 사용자 이름이 그대로 찍히는 개인정보 노출을 피하기 위함.)

**③ AI 제안을 따르지 않은 일 (없으면 이유)**
- **AI 가 제안한 "템플릿 병합 시 기존 id 유지" 를 따르지 않았다.**
  AI 초안은 `importJsonFile` 에서 id 가 겹치면 유지하는 방향을 제시했으나,
  다른 사람이 만든 JSON 을 가져왔을 때 id 가 충돌하면 로컬 템플릿을
  덮어쓸 위험이 있다고 판단하여 **id 가 겹치면 새 `uid()` 를 발급**하도록
  바꿨다. (T03-C23·C24 의 "기존 템플릿 유지" 요건과 합치.)
- **AI 가 제안한 "대용량 이미지도 원본 유지" 를 따르지 않았다.**
  위 ②에서 설명한 다운스케일 정책으로 대체했다.
- **AI 가 제안한 "화면비별 캔버스를 1080/1350/1920 그대로 유지" 는
  따랐으나**, CSS 표시에는 `max-height:70vh` 를 추가로 걸어 세로 9:16 이
  화면을 넘지 않게 했다. (AI 는 이 부분을 명시하지 않았다.)

---

## (C) 제출 패키지 구성

### C-1. 공개 주소 확정 (T03-C01)
- 결과물 주소: **https://sktassign3-imagegen.vercel.app/**
- 소스 주소:  https://github.com/dyj02056/sktassign3_imagegen

**검증 기록 (2026-09-12)**
- 시크릿 창에서 위 주소 접속 → 로그인·인증·초대·OAuth·CAPTCHA 프롬프트 없이
  편집기가 바로 표시됨을 확인.
- 첫 화면에 보이는 요소:
  - 상단: "짤·카드 스튜디오 / 이미지 + 문구 → 밈·카드·SNS 이미지"
  - 헤더 버튼: JSON 내보내기 · JSON 가져오기 · 이미지 다운로드
  - 좌측: 1. 이미지 / 2. 문구 / 3. 배경 패널
  - 가운데: "이미지를 불러오거나 문구를 입력하세요." 빈 상태
  - 우측: 화면비(1:1 · 4:5 · 9:16), 템플릿, 내보내기, 샘플
  - 하단 정보: "비율 1:1 · 캔버스 1080×1080"

→ **T03-C01, T03-C03 통과.**

### C-1-2. 소스 저장소 (제출 시 본인 값으로 채움)
- 저장소: https://github.com/<본인계정>/<저장소명>
- 저장소 루트에 아래 파일이 있어야 함:
  - `index.html` (제출 코드 전문)
  - `README.md`
  - `submission-note.md`
- 저장소 public, README 에 결과물 URL 기재 → T03-C01 만족.

### C-1-3. 배포 절차 (재현용)
1. 로컬에 `index.html` 저장.
2. GitHub 에 public 저장소 생성 후 push.
3. Vercel → New Project → Import Git Repository →
   Framework Preset "Other" → Build Command 비움 → Output Directory 비움 → Deploy.
4. 배포 완료 후 나온 URL 을 상단에 기입.
   (본 제출에서는 https://sktassign3-imagegen.vercel.app/ 로 확정.)

---

### C-2. 완성 이미지 3개 (T03-C25 · C26 · C27)

**파일명 규칙**
final_01_1x1.png ← 1:1 비율, 문구 A
final_02_4x5.png ← 4:5 비율, 문구 B
final_03_9x16.png ← 9:16 비율, 문구 C

서로 다른 **문구 또는 비율**을 반드시 가질 것 (T03-C25).

**생성 절차 (도구 사용)**
1. 배포 URL 접속 (로그인 없음).
2. 우측 "샘플 ① 밈 카드" 클릭 → "이미지 다운로드" → `final_01_1x1.png`.
3. "샘플 ② SNS 카드" 클릭 → `final_02_4x5.png`.
4. "샘플 ③ 세로 스토리" 클릭 → `final_03_9x16.png`.
5. 세 파일을 열어 정상 표시되는지 확인 (T03-C26).

**T03-C27 — 출처·라이선스 기록표**

| 파일 | 본인 제작 여부 | 원본 출처 URL | 사용 허가 근거 |
|---|---|---|---|
| final_01_1x1.png  | 본인 제작 (도구로 직접 생성) | — | — |
| final_02_4x5.png  | 본인 제작 (도구로 직접 생성) | — | — |
| final_03_9x16.png | 본인 제작 (도구로 직접 생성) | — | — |

> 세 장 모두 텍스트·배경색만으로 구성했으므로 외부 이미지 원본이 없다.
> 외부 이미지를 얹어 다시 만들 경우 아래 형식으로 각 행을 채운다:
>
> | 파일 | 본인 제작 | 원본 출처 URL | 사용 허가 근거 |
> |---|---|---|---|
> | final_0X_...png | 아니오 | https://... | CC0 / CC-BY 4.0 / Unsplash License / 수업 제공 자료 등 |

### C-2-2. JSON 3종 검증 (T03-C22 · C23 · C24)

| 파일 | 종류 | 기대 결과 | 실제 결과 |
|---|---|---|---|
| json_valid.json | 정상 JSON | 템플릿 복원 (0 → 3개) | ✅ 통과 |
| json_broken_syntax.json | 문법 손상 (닫는 중괄호 누락) | "JSON 문법 오류 — 기존 템플릿 유지" / 개수 변화 없음 | ✅ 통과 |
| json_missing_templates.json | templates 키 누락 | "필수 항목(templates) 누락 — 기존 템플릿 유지" / 개수 변화 없음 | ✅ 통과 |

**검증 절차**
1. 정상 JSON 가져오기 → 템플릿 목록 3개 표시 확인 (T03-C22)
2. 손상 JSON 가져오기 → 빨간 토스트 + 기존 템플릿 개수 유지 확인 (T03-C23)
3. 필수 누락 JSON 가져오기 → 빨간 토스트 + 기존 템플릿 개수 유지 확인 (T03-C24)

**증거**
- evidence/json/json_valid.json
- evidence/json/json_broken_syntax.json
- evidence/json/json_missing_templates.json
- 각 시나리오 스크린샷


---

### C-3. 공개 안전 점검 (T03-C28 · C29 · C30)

**T03-C28 — 공개 이미지의 위치 정보 메타데이터 0건**

**검증 결과 (2026-09-12)**
- exif.tools 에 final_01_1x1.png 업로드
- 확인된 태그: IHDR/PNG 7건 (Bit Depth, Color Type, Compression, Filter,
  Image Height/Width, Interlace) + 기타 13건 = 총 20건
- GPS / Location / 카메라 / 촬영 정보 태그: 0건
- 결론: T03-C28 통과
- 증거: evidence/exif_final_01.png (스크린샷)
**검증 결과 (2026-09-12)**
- exif.tools 에 final_02_4x5.png 업로드
- 확인된 태그: IHDR/PNG 7건 (Bit Depth, Color Type, Compression, Filter,
  Image Height/Width, Interlace) + 기타 13건 = 총 20건
- GPS / Location / 카메라 / 촬영 정보 태그: 0건
- 결론: T03-C28 통과
- 증거: evidence/exif_final_02.png (스크린샷)
**검증 결과 (2026-09-12)**
- exif.tools 에 final_01_9x16.png 업로드
- 확인된 태그: IHDR/PNG 7건 (Bit Depth, Color Type, Compression, Filter,
  Image Height/Width, Interlace) + 기타 13건 = 총 20건
- GPS / Location / 카메라 / 촬영 정보 태그: 0건
- 결론: T03-C28 통과
- 증거: evidence/exif_final_03.png (스크린샷)




**T03-C29 — 공개 화면·제출물 개인정보 0건**
- 코드에 사용자 이름·이메일·전화 등 하드코딩 없음.
- A 단계에서 파일명을 `imgInfo` 텍스트가 아닌 title 속성으로만 노출하도록
  수정 → 스크린샷에 사용자 홈 디렉터리명이 찍히지 않음.
- 제출 전 확인: 브라우저 주소창·탭 제목·콘솔·네트워크 탭에 개인정보 없음.

**T03-C30 — 비밀값 원문 0건**
- API 키·토큰·비밀번호·.env 파일 없음.
- 배포는 Vercel 정적 호스팅이며 서버 환경변수 사용 안 함.
- 저장소에 `.env`, `*.key`, `credentials.json` 등 없음
  (제출 직전 `git status` 확인).

---

### C-4. 제출물 체크리스트 (최종)
- 결과물 주소
   https://sktassign3-imagegen.vercel.app/
- 소스 주소 — (https://github.com/dyj02056/sktassign3_imagegen)
- 짧은 확인 방법 4줄

   ▸ 어디로 가나요
     https://sktassign3-imagegen.vercel.app/ (로그인 없이 열림)

   ▸ 3단계 이내 무엇을 하나요
     1) 좌측 상단 "PNG · JPEG 불러오기" 클릭 → 이미지 선택
     2) 문구 상자에 "안녕 😀" 입력 후 크기 슬라이더 이동
     3) 우측 상단 "이미지 다운로드" 클릭

   ▸ 무엇이 보이면 통과인가요
     - 좌측 라벨: "✅ 이미지 로드됨 (W×H)" 로 바뀜
     - 슬라이더 이동 시 캔버스 문구가 즉시 따라 움직임
     - 다운로드 파일이 미리보기와 문구 위치·줄바꿈·잘림이 동일

   ▸ 안 될 때 무엇이 보이나요
     - 지원하지 않는 파일: 빨간 토스트
       "지원하지 않는 형식: <MIME> (PNG·JPEG·WebP만)"
       + 좌측 라벨 "❌ 지원하지 않는 파일 (기존 작업 유지)"
       → 기존 캔버스 작업은 그대로 유지됨
     - 손상 JSON: "JSON 문법 오류 — 기존 템플릿 유지"
     - 필수 누락 JSON: "필수 항목(templates) 누락 — 기존 템플릿 유지"
- AI와 나의 판단 3줄

   ▸ AI에게 맡긴 일
     - HTML/CSS/JS 3분할 구조 설계 및 초기 스캐폴딩
     - wrapTextWith(context, ...) 줄바꿈 알고리즘 초안
     - computeImageRect cover-fit 계산식
     - localStorage 템플릿 CRUD, JSON I/O, 다운로드 파이프라인 뼈대
     - T03-C01~C32 각 항목과 코드 라인의 1차 대조표

   ▸ 학생이 직접 판단한 일
     - 미리보기·저장 파일의 폰트 측정을 같은 컨텍스트에서 해야 한다고 판단
       → wrapTextWith(octx, ...) 로 강제 분리
     - 20MB 이상 이미지를 최대 변 2400px 로 다운스케일
       (EXIF 이중 제거 + 성능, T03-C28 함께 충족)
     - 템플릿 "수정" 을 두 경로(이름 더블클릭 + 덮어쓰기)로 제공
     - 다운로드 파일명에 downloadSeq + 4자리 랜덤 결합
     - 파일명을 화면 텍스트가 아닌 title 속성으로만 노출
     - 단일 HTML 을 3개 파일로 분리 (유지보수성)

   ▸ AI 제안을 따르지 않은 일
     - "템플릿 병합 시 기존 id 유지" 거부 → id 충돌 시 새 uid 발급
     - "대용량 이미지도 원본 유지" 거부 → 2400px 다운스케일로 대체
     - "단일 index.html 유지" 거부 → 3분할 (배포·로컬 실행 손해 없음)
- 카드 1 (T03-C04, C05, C09, C10). 코드 + 배포 URL 실기 확인.
-  카드 2 (T03-C11~C13). `wrapTextWith(octx, ...)` 컨텍스트 분리로
        미리보기·저장 파일의 폰트 측정 경로 통일.
        `final_01_1x1.png` / `final_02_4x5.png` / `final_03_9x16.png` 3장에서 확인.
- 극단 입력 12건 검사 — (C-5) 표
- 템플릿 생성·불러오기·수정·삭제 유지
- C-2-2 (JSON 3종 검증) 완료
- 완성 이미지 3개 파일
- 출처·라이선스 기록
- 위치정보 메타데이터 0건
- 개인정보·비밀값 0건

---

### C-5. 극단 입력 12건 검사 결과표 (T03-C14 ~ C25)

아래 표를 실제로 배포 URL 에서 수행한 뒤 결과를 채워 제출한다.
"통과" 는 도구가 멈추지 않고, 다운로드 파일이 미리보기와 일치함을 뜻한다.

| # | 입력 / 조건 | 대응 기준 | 결과 |
|---|---|---|---|
| 1 | 200자 이상 긴 문구 | T03-C14 | ☐ 통과 
| 2 | 공백만 있는 문구("   ") | T03-C15 | ☐ 통과 
| 3 | 특수문자 + 이모지("!@#$😀🎉") | T03-C16 | ☐ 통과 
| 4 | 문구 크기 최소(4px) | T03-C17 | ☐ 통과 
| 5 | 문구 크기 최대(400px) | T03-C18 | ☐ 통과 
| 6 | 문구를 이미지 밖(x=-100%)으로 이동 | T03-C19 | ☐ 통과
| 7 | 100×100 미만 이미지 로드 | T03-C20 | ☐ 통과 /
| 8 | 20MB 이상 이미지 로드 | T03-C21 | ☐ 통과 / 
| 9 | 위 조건에서 다운로드 = 미리보기 | T03-C22 | ☐ 통과 
| 10 | 창 크기 조절(가로↔세로) | T03-C23 | ☐ 통과 / 
| 11 | 모바일 폭 360px (DevTools) | T03-C24 | ☐ 통과 /
| 12 | 연속 다운로드 3회 | T03-C25 | ☐ 통과 / 

**수행 메모**
- 4~5번: 슬라이더 양 끝까지 이동 후 다운로드 → 파일 열어 미리보기와 대조.
- 6번: x 슬라이더를 -100% 로 이동 → 문구가 화면 밖으로 나가도 콘솔 오류 0건.
- 7~8번: 필요하면 테스트 이미지를 만들어 사용 (100×100 PNG, 25MB JPEG).
- 10번: 창을 좁혔다 넓혔다 반복 → 문구 위치·비율 유지 확인.
- 11번: Chrome DevTools → Toggle device toolbar → 360px 커스텀.
- 12번: 다운로드 버튼을 빠르게 3회 → 파일명이 서로 다르고 모두 저장됨을 확인.

---

### C-6. 최종 제출물 요약

결과물 주소: https://sktassign3-imagegen.vercel.app/

소스 주소: https://github.com/dyj02056/sktassign3_imagegen

짧은 확인 방법 4줄 → (B-1)

AI와 나의 판단 3줄 → (B-2)

첨부: final_01_1x1.png / final_02_4x5.png / final_03_9x16.png
